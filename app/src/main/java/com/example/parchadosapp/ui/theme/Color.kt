package com.example.parchadosapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores basada en tu esquema
val PrimaryColor = Color(0xFF003F5C)  // Azul profundo (para encabezados, botones principales y elementos clave)
val SecondaryColor = Color(0xFF2F4B7C) // Azul más claro (para menús secundarios, bordes y hover)
val AccentColor = Color(0xFFEAC67A)  // Amarillo dorado (para botones CTA, iconos destacados y llamados de atención)
val BackgroundColor = Color(0xFFF8F5F0) // Beige claro (fondo principal)
val TextColor = Color(0xFF1E293B)  // Gris oscuro (para textos en fondos claros)
val DetailColor = Color(0xFFA5C8E1)  // Azul pastel (para elementos decorativos y detalles sutiles)

// Extras
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
val Red = Color(0xFFE63946)

